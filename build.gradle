buildscript {
    repositories {
        // These repositories are now declared globally in settings.gradle
        // google() // REMOVED
        mavenCentral() // Keep this if you have specific dependencies that only live here, otherwise it can also be removed if declared in settings.gradle
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.0'
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0'
        classpath 'com.google.gms:google-services:4.4.1'
    }
}

allprojects {
    repositories {
        // These repositories are now declared globally in settings.gradle
        // google() // REMOVED
        mavenCentral() // Keep this if you have specific dependencies that only live here, otherwise it can also be removed if declared in settings.gradle
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}